# Welfamily 機能要求書 (Functional Requirements Document)

## 1. 概要 (Overview)
「Welfamily」は、夫婦・パートナー関係に悩むユーザーに対して、日々の悩みや心配事に優しく寄り添い、ワンポイントアドバイスを提供する「心の伴走者」となるWebアプリケーションである。本要求書は、開発仕様書に基づき、システムが満たすべき機能および非機能要件を定義する。

## 2. ユーザーインターフェース (UI) 要求
* **チャットインターフェース**: ユーザーが直感的に悩みやメッセージを入力できる、LINEや一般的なメッセンジャーアプリのようなチャット形式の画面を提供する。
* **対話履歴の表示**: ユーザーの発言とAI（Welfamily）の応答を画面上にタイムライン形式で表示し、会話の文脈を視覚的に把握できるようにする。
* **シンプルな操作性**: ユーザーにAPIキー等のシステム設定を入力させる画面は設けず、エンドユーザーは純粋にチャット機能のみを利用できる設計とする。

## 3. システム・AI 機能要求
* **利用AIモデル**: Google Gemini API の `gemini-2.5-flash` モデルを採用する。
* **システムプロンプト (System Instruction) 要件**:
    AIの応答品質とキャラクターを担保するため、初期化時に以下のルールとナレッジをシステムプロンプトとして組み込むこと。
    1. **ペルソナ**: 「Welfamily」というブランドを体現する、優しく寄り添う心の伴走者として振る舞うこと。
    2. **完全な受容と共感**: ユーザーの感情（不安、怒りなど）を絶対に否定せず、まずは深く受け止めて徹底的に共感すること。
    3. **「心の通信線」の構築**: 問題解決や正論（事実の通信線）を急ぐのではなく、女性脳の共感欲求の高さを踏まえ、相手の心を肯定する「心の通信線」を繋ぐことを最優先とすること。
    4. **ワンポイントアドバイス**: 十分な共感を示した上で、長すぎない温かいトーンで、今日から実践できる小さな「ワンポイントアドバイス」を添えること。
* **指定ナレッジベースの活用**:
    夫婦関係改善サービスの知見（記録フォルダ1、2の内容や、男女の会話の目的の違い、不満の蓄積問題など）をAIのベース知識として活用し、応答に反映させる。

## 4. セキュリティ・非機能要求
* **APIキーのセキュア管理**: Gemini APIの認証キー（`GEMINI_API_KEY`）は、ハードコードやユーザー入力（画面入力）を避け、StreamlitのSecrets機能（`st.secrets["GEMINI_API_KEY"]`）を用いてバックエンドで安全に参照する。
* **機密情報の漏洩防止**: ローカル検証時に作成する `.streamlit/secrets.toml` 等の認証情報を含むファイルは、必ず `.gitignore` に追加し、リモートリポジトリ（Git）に流出しないように制御する。
* **フレームワーク**: フロントエンドおよびバックエンドは Python, Streamlit で実装し、ローカル環境（`streamlit run app.py`）で安定して起動・動作すること。
* **自己修復・エラーハンドリング**: 実行時にエラーが発生した場合や、応答が要件を満たさない場合は、AIエージェントが自律的にエラーの原因を特定・修復できる仕組み（またはそれに準ずる堅牢なコード設計）を備えること。

# Welfamily 非機能要求書 (Non-Functional Requirements Document)

## 1. 概要 (Overview)
本ドキュメントは、「Welfamily」Webアプリケーションにおける非機能要件を定義する。ユーザーがいつでも安心して、安全かつ快適に「心の伴走者」であるAIと対話できるよう、技術的・運用的な品質基準を策定する。

## 2. ユーザビリティ・デザイン (Usability & Design)
* **マルチデバイス対応 (レスポンシブデザイン)**:
    * 主なユーザー層はスマートフォンからのアクセスを想定しているため、モバイル端末（iOS/Android）のブラウザで完全に最適化された表示・操作感（レスポンシブ対応）を保証する。
* **親しみやすく落ち着いたビジュアル設計**:
    * 悩みを抱えるユーザーの心を落ち着かせるため、刺激的な色彩（純粋な赤や青など）を避け、暖かみのあるパステルカラーやアースカラー、調和のとれた配色スケームを採用する。
    * フォントサイズや行間は可読性を重視し、スマートフォンの画面でも長文がストレスなく読めるレイアウトにする。
* **心理的負担の軽減**:
    * 操作ミスを防ぐため、ボタンの配置や導線を極力シンプルにする。

## 3. 性能と効率性 (Performance & Efficiency)
* **レスポンス時間と体感速度の向上**:
    * 生成AI（Gemini）によるテキスト生成は時間がかかる場合があるため、Streamlitのストリーミング表示機能（`st.write_stream` 等）を活用し、リアルタイムで回答が生成される様子を見せることで、ユーザーの待機ストレス（体感応答時間）を最小化する。
    * 初期ローディング時間は3秒以内を目指す。
* **同時接続への配慮**:
    * Streamlitの特性を踏まえ、複数ユーザーが同時にアクセスした際も、セッション（`st.session_state`）が他者と混ざることなく、完全に独立して処理される状態を担保する。

## 4. セキュリティとプライバシー (Security & Privacy)
* **機密情報の安全な管理**:
    * Gemini APIのAPIキー（`GEMINI_API_KEY`）などの認証情報は、ソースコード内にハードコードせず、StreamlitのSecrets管理機構（ローカルでは `.streamlit/secrets.toml`、デプロイ先ではプラットフォーム環境変数）を利用してセキュアに読み込む。
    * バージョン管理システム（Git/GitHub）へシークレット情報が流出しないよう、`.gitignore` に秘密情報ファイルを厳格に登録する。
* **通信の暗号化**:
    * ユーザーとサーバー間のすべての通信は、HTTPSにより暗号化される環境でホストする。
* **データプライバシーと対話データの保護**:
    * ユーザーが入力した悩みごとの内容は、原則としてセッション内（ブラウザを閉じるまで）の一時メモリ上でのみ保持し、サーバー側に恒久的に保存しない設計とすることで、最高レベルのプライバシー保護を実現する。
    * （将来的にログを蓄積する場合は、個人を特定できる情報（PII）を一切含まない形式でアノニマイズ化する設計とする。）

## 5. 信頼性と可用性 (Reliability & Availability)
* **可用性 (Availability)**:
    * 夫婦関係の悩みは夜間や休日に顕在化しやすいため、原則として24時間365日の稼働を目指す。
* **エラーハンドリングとフォールバック (Fallback)**:
    * APIの制限超過（Rate Limit）やサーバー障害、ネットワーク切断などの例外発生時に、システムのエラートレース（生のエラー文）をそのまま画面に表示しない。
    * エラー時には、「申し訳ありません、現在少しお返事の準備に時間がかかっています。少し時間をおいてから、もう一度お話しを聞かせてくださいね。」といった、キャラクター（Welfamily）のトーンに合わせた温かく親切なフォールバックメッセージを表示する。

## 6. 保守性と互換性 (Maintainability & Compatibility)
* **コードの簡潔性とモジュール化**:
    * ソースコードは Python 3.10 以上で動作し、極力シンプルで可読性の高い構成（`app.py` への集約または明確な役割分担）を維持する。
    * 依存パッケージは `requirements.txt` にバージョンを明記して管理し、将来的なアップデートによる動作不良を防ぐ。
* **動作環境の互換性**:
    * 主要なウェブブラウザ（Google Chrome, Safari, Microsoft Edge, Firefox）の最新バージョンで正常に機能すること。
